export enum PublicRoutes {
  LOGIN = "/login",
  REGISTER = "/register",
}
export enum PrivateRoutes {
  DISCORDIA = "/discordia",
  HOME = "home",
  CHAT_SERVER = "chat_server",
  CHAT_FRIEND = "chat_friend",
  SERVER = "server",
}
